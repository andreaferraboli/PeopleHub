<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name=".BirthdayWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_birthdays_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/birthday_widget_info" />
        </receiver>

        <receiver
            android:name=".CheckInWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_checkins_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/checkin_widget_info" />
        </receiver>

        <receiver
            android:name=".PinnedEventWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_event_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/pinned_event_widget_info" />
        </receiver>
    </application>

</manifest>
